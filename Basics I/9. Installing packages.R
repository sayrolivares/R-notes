#Packages
#Attach the data
attach(online_retail)

#Check the variable names
names(online_retail)

#Install packages
help(install.packages)

#Install epiR package
install.packages("epirR")

#Install packages available
install.packages()

#Load library
library(epiR)
